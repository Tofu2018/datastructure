#include <cstdlib>
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

void input(){
	ofstream analyze;
	analyze.open(file/analyze/example.txt,ios::app);
	if(analyze.fail()){
		system("cd. < file/analyze/example.txt")
	}
	
	string a;
	while(cin >> a){
		if(a!='.'){
			cout << "End Input!!" << endl;
			return 0;
		}else{
			
		}
	}
}

void treemaker(){
	
}
